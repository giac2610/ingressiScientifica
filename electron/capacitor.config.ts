import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scientifica.ingressi',
  appName: 'ingressiScientifica',
  webDir: 'www'
};

export default config;
